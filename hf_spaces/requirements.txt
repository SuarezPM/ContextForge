# HuggingFace Spaces runtime requirements — scoped down from the
# parent repo's requirements.txt. The Space runs on CPU-only HF free
# tier, so we drop torch / sentence-transformers / faiss-gpu and keep
# only what `demo/app.py` actually needs to render the dashboard +
# exercise the LSH / FAISS / JCR / TokenDance code paths.
#
# Pinned ranges follow the parent repo to avoid drift; the upper
# bound of each range is the one the project has tested.

# Web framework + UI
gradio>=6.0,<7.0
plotly>=5.24,<7.0
fastapi>=0.115,<0.137
uvicorn[standard]>=0.32,<0.50
pydantic>=2.9,<3
pydantic-settings>=2.6,<3
httpx>=0.27,<0.30

# Tokenization + compression
transformers>=4.46,<5
numpy>=1.26,<3
aiofiles>=24.1,<25
psutil>=5.9,<8
prometheus-client>=0.19.0,<1

# Math / dedup
faiss-cpu>=1.7.4

# Optional, kept light on Spaces:
#  - sentence-transformers, llmlingua, torch  → omitted; the demo
#    falls back to xorshift pseudo-embeddings if those are missing.
#  - vllm                                      → omitted; the Space
#    does not run vLLM (no GPU on free tier).
